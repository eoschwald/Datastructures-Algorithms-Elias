$$
\begin{aligned}
5n + 3 &\in O(n) && \text{(wahr)} \\
\frac{n}{3} &\in O(n) && \text{(wahr)} \\
n \cdot n &= n^2 \in O(n^2) && \text{(wahr)} \\
n + n + n &\in O(n) && \text{(wahr)} \\
2n + 2n &\in O(n^2) && \text{(falsch)} \\
\frac{n}{2} + n &\in O(n) && \text{(wahr)}
\end{aligned}
$$
